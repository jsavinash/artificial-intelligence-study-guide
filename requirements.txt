# Runtime deps for the AI tutorial monorepo (capacity check:
# ALL of these are already installed on this machine — `make setup` verifies)
numpy>=2.0
pandas>=2.3
scikit-learn>=1.6
scipy>=1.13
matplotlib>=3.9
pytest>=8.4

# Optional (not required — examples fall back to MockLLM / NumPy when absent):
# openai>=1.40
# anthropic>=0.34
#
# PyTorch is installed on THIS machine (torch 2.14.0, Apple M1 MPS) and is the
# *primary* training backend: 19 of 28 examples train with autograd via
# ai_core.torch_backend (MLP, CNN, RNN, transformers, VAE/GAN/diffusion, DQN,
# LoRA, CLIP, InfoNCE retriever, GNN, matrix factorization...).
#
# It stays OUT of the required list on purpose — everything still passes without
# it: each torch_backend function ships a genuine NumPy fallback running the same
# algorithm (verified parity in both modes: run-all 28/28, validator 31/31 torch
# and 26/31+5-skip numpy). Without torch you also skip the ~1.24s import cost
# that module 27 measures — relevant for sub-second examples. Install with:
#   python3 -m pip install torch     # ~590MB
# See docs/curriculum/27-accelerated-computing-and-pytorch.md and `make bench`.
# torch>=2.6

